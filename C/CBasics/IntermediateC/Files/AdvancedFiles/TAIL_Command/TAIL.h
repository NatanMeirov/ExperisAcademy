#ifndef __TAIL_H__
#define __TAIL_H__

/* Shows all the lines in a given file, from the N line till the EOF, returns status (0 - if succeed, 1 - if failed) */
int ShowTail(char* _textFileName, char* _lines);


#endif /* #ifndef __TAIL_H__ */
