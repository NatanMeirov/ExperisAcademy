#ifndef __WC_H__
#define __WC_H__


/* Counting Lines, Words, Characters of a given file- and prints them to stdout (Screen), returning a status (0 - if succeed, 1 - if failed) */
int WordsCount(char* _textFileName);

#endif /* #ifndef __WC_H__ */
